# --- LangChain core + LangGraph ---
langchain>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# --- Local LLM + embeddings via Ollama ---
langchain-ollama>=0.2.0

# --- Vector store ---
chromadb>=0.5.0
langchain-chroma>=0.1.0

# --- Document loading (PDF/DOCX) ---
pypdf>=4.0.0
docx2txt>=0.8

# --- Structured data schemas ---
pydantic>=2.7.0

# --- Numeric ops (cosine similarity in the scoring node) ---
numpy>=1.26.0

# --- Utilities ---
python-dotenv>=1.0.0
